//#region dist/.tsdown-types-es/types.d.ts
interface CacheStrategy {
	get(key: string): Promise<string | null>;
	set(key: string, value: string, ttl?: number): Promise<void>;
}
interface TranscriptConfig {
	lang?: string;
	userAgent?: string;
	cache?: CacheStrategy;
	cacheTTL?: number;
	disableHttps?: boolean;
	fetch?: typeof globalThis.fetch;
	videoFetch?: (params: {
		url: string
		lang?: string
		userAgent?: string
	}) => Promise<Response>;
	transcriptFetch?: (params: {
		url: string
		lang?: string
		userAgent?: string
	}) => Promise<Response>;
}
interface TranscriptSegment {
	text: string;
	duration: number;
	offset: number;
	lang?: string;
}
type TranscriptResponse = {
	title: string
	segments: TranscriptSegment[]
};

//#endregion
//#region dist/.tsdown-types-es/cache/fs-cache.d.ts
declare class FsCache implements CacheStrategy {
	private cacheDir;
	private defaultTTL;
	constructor(cacheDir?: string, defaultTTL?: number);
	get(key: string): Promise<string | null>;
	set(key: string, value: string, ttl?: number): Promise<void>;
}

//#endregion
//#region dist/.tsdown-types-es/cache/in-memory-cache.d.ts
declare class InMemoryCache implements CacheStrategy {
	private cache;
	private defaultTTL;
	constructor(defaultTTL?: number);
	get(key: string): Promise<string | null>;
	set(key: string, value: string, ttl?: number): Promise<void>;
}

//#endregion
//#region dist/.tsdown-types-es/errors.d.ts
declare class YoutubeTranscriptTooManyRequestError extends Error {
	constructor();
}
declare class YoutubeTranscriptVideoUnavailableError extends Error {
	constructor(videoId: string);
}
declare class YoutubeTranscriptDisabledError extends Error {
	constructor(videoId: string);
}
declare class YoutubeTranscriptNotAvailableError extends Error {
	constructor(videoId: string);
}
declare class YoutubeTranscriptNotAvailableLanguageError extends Error {
	constructor(lang: string, availableLangs: string[], videoId: string);
}
declare class YoutubeTranscriptInvalidVideoIdError extends Error {
	constructor();
}

//#endregion
//#region dist/.tsdown-types-es/index.d.ts
/**
* Implementation notes:
* - Keeps the public surface identical.
* - Internals now use YouTube Innertube `player` to discover captionTracks instead of scraping the watch HTML.
* - Honors `lang`, custom fetch hooks (`videoFetch`, `transcriptFetch`), and optional cache strategy.
*/
/**
* Implementation notes:
* - Keeps the public surface identical.
* - Internals now use YouTube Innertube `player` to discover captionTracks instead of scraping the watch HTML.
* - Honors `lang`, custom fetch hooks (`videoFetch`, `transcriptFetch`), and optional cache strategy.
*/
declare class YoutubeTranscript {
	private config?;
	private fetch;
	constructor(config?: TranscriptConfig & {
		cacheTTL?: number
	});
	fetchTranscript(videoId: string): Promise<TranscriptResponse>;
	static fetchTranscript(videoId: string, config?: TranscriptConfig): Promise<TranscriptResponse>;
}
declare const fetchTranscript: (videoId: string, config?: TranscriptConfig) => Promise<TranscriptResponse>;

//#endregion
export { CacheStrategy, FsCache, InMemoryCache, TranscriptConfig, TranscriptResponse, TranscriptSegment, YoutubeTranscript, YoutubeTranscriptDisabledError, YoutubeTranscriptInvalidVideoIdError, YoutubeTranscriptNotAvailableError, YoutubeTranscriptNotAvailableLanguageError, YoutubeTranscriptTooManyRequestError, YoutubeTranscriptVideoUnavailableError, fetchTranscript };